version: '3.8'

services:
  apache-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: apache-server:latest
    container_name: apache-server
    ports:
      - "1000:80"
    restart: unless-stopped
